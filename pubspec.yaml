name: societyos_guard
description: SocietyOS Guard app - Flutter implementation (starter)
publish_to: 'none'
version: 0.1.0
environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.3.6
  http: ^0.13.6
  web_socket_channel: ^2.3.0
  flutter_secure_storage: ^8.0.0
  qr_code_scanner: 1.0.1
  camera: ^0.10.0+6
  firebase_messaging: ^14.2.4
  flutter_local_notifications: ^13.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
